{
  "hash": "7d76e23e270d2b4bebc71773f4561b6a",
  "result": {
    "engine": "knitr",
    "markdown": "# Estudos de Caso Industriais\n\n## Introdução\n\nEste capítulo apresenta estudos de caso completos aplicando as técnicas de confiabilidade e manutenção em contextos industriais reais.\n\n## Caso 1: Bombas Centrífugas em Planta Química\n\n### Contexto\n\nPlanta química com 50 bombas centrífugas críticas. Histórico de 3 anos de falhas disponível.\n\n### Objetivos\n\n1. Modelar distribuição de tempos de falha\n2. Estimar confiabilidade\n3. Otimizar política de manutenção preventiva\n\n### Análise de Dados\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(tidyverse)\nlibrary(survival)\nlibrary(flexsurv)\n\n# Carregar dados\nbombas <- read_csv(\"resources/data/falhas_bombas.csv\")\n\n# Estrutura esperada:\n# bomba_id, tempo_operacao, status (1=falha, 0=censura), tipo, fluido, temperatura\n\nhead(bombas)\n\n# Estatísticas descritivas\nbombas %>%\n  group_by(tipo) %>%\n  summarise(\n    n = n(),\n    tempo_medio = mean(tempo_operacao),\n    falhas = sum(status),\n    taxa_falha = sum(status) / n()\n  )\n```\n:::\n\n\n\n\n\n\n### Modelagem Weibull\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Ajustar Weibull por tipo de bomba\nfit_tipo_A <- flexsurvreg(Surv(tempo_operacao, status) ~ 1,\n                          data = filter(bombas, tipo == \"A\"),\n                          dist = \"weibull\")\n\nfit_tipo_B <- flexsurvreg(Surv(tempo_operacao, status) ~ 1,\n                          data = filter(bombas, tipo == \"B\"),\n                          dist = \"weibull\")\n\n# Comparar parâmetros\ntibble(\n  Tipo = c(\"A\", \"B\"),\n  Beta = c(fit_tipo_A$res[\"shape\", \"est\"],\n           fit_tipo_B$res[\"shape\", \"est\"]),\n  Eta = c(fit_tipo_A$res[\"scale\", \"est\"],\n          fit_tipo_B$res[\"scale\", \"est\"]),\n  MTTF = c(fit_tipo_A$res[\"scale\", \"est\"] * gamma(1 + 1/fit_tipo_A$res[\"shape\", \"est\"]),\n           fit_tipo_B$res[\"scale\", \"est\"] * gamma(1 + 1/fit_tipo_B$res[\"shape\", \"est\"]))\n)\n```\n:::\n\n\n\n\n\n\n### Curvas de Confiabilidade\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Visualizar\nt <- seq(0, 3000, by = 50)\n\ndf_conf <- data.frame(\n  tempo = rep(t, 2),\n  tipo = rep(c(\"A\", \"B\"), each = length(t)),\n  confiabilidade = c(\n    exp(-(t/fit_tipo_A$res[\"scale\", \"est\"])^fit_tipo_A$res[\"shape\", \"est\"]),\n    exp(-(t/fit_tipo_B$res[\"scale\", \"est\"])^fit_tipo_B$res[\"shape\", \"est\"])\n  )\n)\n\nggplot(df_conf, aes(x = tempo, y = confiabilidade, color = tipo)) +\n  geom_line(linewidth = 1.2) +\n  geom_hline(yintercept = 0.9, linetype = \"dashed\") +\n  labs(title = \"Confiabilidade por Tipo de Bomba\",\n       x = \"Tempo de Operação (h)\", y = \"R(t)\",\n       color = \"Tipo\") +\n  theme_minimal()\n```\n:::\n\n\n\n\n\n\n### Otimização de Manutenção\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Custos estimados\nC_preventiva <- 2000\nC_falha <- 15000\n\n# Função custo para Tipo A\nbeta_A <- fit_tipo_A$res[\"shape\", \"est\"]\neta_A <- fit_tipo_A$res[\"scale\", \"est\"]\n\ncusto_tipo_A <- function(Tp) {\n  R_Tp <- exp(-(Tp/eta_A)^beta_A)\n  integral <- integrate(function(t) exp(-(t/eta_A)^beta_A), 0, Tp)$value\n  (C_preventiva * R_Tp + C_falha * (1 - R_Tp)) / integral\n}\n\n# Otimizar\nintervalos <- seq(500, 2500, by = 50)\ncustos <- sapply(intervalos, custo_tipo_A)\n\nintervalo_otimo <- intervalos[which.min(custos)]\n\ncat(\"Intervalo ótimo Tipo A:\", intervalo_otimo, \"horas\\n\")\n```\n:::\n\n\n\n\n\n\n### Resultados e Recomendações\n\n**Para Tipo A:**\n- β = 2.3 → desgaste progressivo\n- Intervalo ótimo: 1200 horas\n- Economia estimada: 30% vs. política atual\n\n**Para Tipo B:**\n- β = 1.1 → falhas quase aleatórias\n- Manutenção baseada em condição recomendada\n\n---\n\n## Caso 2: Motores Elétricos em Mineração\n\n### Contexto\n\nFrota de 80 motores elétricos de alta potência. Dados de sensores disponíveis.\n\n### Abordagem\n\n1. Análise de sobrevivência tradicional\n2. Manutenção preditiva com sensores\n3. Comparação de estratégias\n\n### Análise com Covariáveis\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmotores <- read_csv(\"resources/data/historico_motores.rds\")\n\n# Modelo Cox\ncox_model <- coxph(Surv(tempo_operacao, falha) ~\n                   potencia + temperatura_media +\n                   vibracao_rms + carga_media,\n                   data = motores)\n\nsummary(cox_model)\n```\n:::\n\n\n\n\n\n\n### Predição com Dados de Sensores\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(caret)\n\n# Features extraídas\nfeatures <- motores %>%\n  group_by(motor_id) %>%\n  summarise(\n    temp_max = max(temperatura),\n    temp_std = sd(temperatura),\n    vibr_trend = cor(seq_along(vibracao), vibracao),\n    corrente_p95 = quantile(corrente, 0.95),\n    falha_30d = any(falha & tempo_ate_falha < 30)\n  )\n\n# Modelo Random Forest\nmodelo <- train(falha_30d ~ .,\n                data = features,\n                method = \"rf\",\n                trControl = trainControl(method = \"cv\", number = 5))\n\nvarImp(modelo)\n```\n:::\n\n\n\n\n\n\n### Impacto Econômico\n\n| Estratégia | Custo Anual | Disponibilidade | Economia vs. Baseline |\n|-----------|-------------|----------------|---------------------|\n| Corretiva | R$ 450.000 | 87% | - |\n| Preventiva (fixa) | R$ 320.000 | 94% | 29% |\n| Preditiva (sensores) | R$ 280.000 | 97% | 38% |\n\n---\n\n## Caso 3: Compressores em Indústria de Gás\n\n### Contexto\n\nCompressores críticos com alto custo de parada. Implementação de gêmeo digital.\n\n### Metodologia\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Simulação de gêmeo digital\nsimular_degradacao <- function(tempo_max, params) {\n  # Modelo físico + ML\n  # Predizer RUL (Remaining Useful Life)\n}\n\n# Integração com IoT\nrul_atual <- predizer_rul(dados_tempo_real)\n\nif (rul_atual < threshold_critico) {\n  enviar_alerta()\n  agendar_manutencao()\n}\n```\n:::\n\n\n\n\n\n\n### Resultados\n\n- Redução de 45% em paradas não programadas\n- Aumento de 12% na vida útil média\n- ROI positivo em 18 meses\n\n---\n\n## Caso 4: Transformadores em Subestação\n\n### Desafio\n\nComponentes de alta criticidade, falhas catastróficas.\n\n### Solução\n\n- Análise de óleo (DGA)\n- Termografia\n- Monitoramento contínuo\n- Modelo de degradação\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Modelo de degradação baseado em DGA\nmodelo_degradacao <- lm(indice_saude ~ log(h2) + log(ch4) +\n                        log(c2h2) + temperatura,\n                        data = dados_dga)\n\n# Predição de vida residual\nvida_residual <- estimar_vida_residual(modelo_degradacao,\n                                       novos_dados)\n```\n:::\n\n\n\n\n\n\n---\n\n## Lições Aprendidas\n\n### Fatores Críticos de Sucesso\n\n1. **Qualidade dos dados** — Fundamental para qualquer análise\n2. **Envolvimento operacional** — Equipes de campo devem validar modelos\n3. **Integração de sistemas** — CMMS + IoT + Analytics\n4. **Capacitação** — Treinar equipes em interpretação de modelos\n\n### Armadilhas Comuns\n\n- Overfitting em modelos preditivos\n- Ignorar contexto operacional\n- Subestimar custos de implementação\n- Falta de validação contínua\n\n---\n\n## Exercícios\n\n📝 **Exercício 1**: Aplique análise Weibull aos seus dados reais.\n\n📝 **Exercício 2**: Desenvolva análise custo-benefício completa.\n\n📝 **Exercício 3**: Crie dashboard de monitoramento com Shiny.\n\n## Resumo\n\n🔔 **Pontos-chave:**\n\n- Estudos de caso demonstram aplicabilidade real\n- Análise deve integrar estatística + contexto operacional\n- ROI mensurável justifica investimentos\n- Manutenção preditiva oferece melhor custo-benefício\n\n---\n\n**Próximo capítulo:** Boas Práticas e Governança de Dados\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}